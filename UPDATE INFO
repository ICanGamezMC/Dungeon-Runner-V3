When updating check

function/player_stats/max_stats  ( For nbt file changes )
dungeon/loot_table/items ( For json file data, Make sure that it contains all the data in the file when updating)
run /function admin:error_test and /function admin:debug ( This is to debug and test all functions)
